---
name: raineye-python-test
description: Run the bundled RainEye Python script when asked to verify that an OpenCode Skill can execute a script.
---

# RainEye Python Skill Test

Run the following command from the project root:

```text
python .opencode/skills/raineye-python-test/scripts/test_success.py
```

Return the command's standard output to the user. The expected output is exactly `测试成功`.
