
实现一个vscode插件，实现用opencode写代码的功能。要求插件界面和codebuddy类似，案例参考如图。期望实现以下目标：

**1、能够支持自动连接系统已有的opencode进程，没有opencode进程则启动一个新的进程。
    **1)自动识别已有的opencode进程
    **2)弹窗提示用户识别到opencode进程，有两个选择：连接和启动新进程
    **3)如果用户选择连接，则连接已有的opencode进程
    **4)如果用户选择启动新进程，则启动一个新的opencode进程
**2、支持选择已有模型和添加自定义模式
    **1)需要兼容openAI协议和Anthropic协议
    **2)已有模型需要包括主流的中国国产模型(deepseek，GLM，qwen，kimi等)和主流国外模型(gpt,claude)
    **3)支持添加自定义模型，需要提供baseurl和apikey等关键信息的配置

**3、支持选择agent模式。
    **1)可以选择plan和craft模式(使用下拉框)
    **2)plan模式下，仅可读取或在用户允许的情况下写入方案文档(用户点击✅表示同意写入文件，点击❌表示不同意)。plan模式需要拆解用户的需求，转化为可一步一步执行的方案，方便后续执行。同时输出需求后，给出按钮，执行或者继续讨论
    **3)craft模式下，可以任意读取和新建和修改，需要给出文件的diff(各个文件增加或删减的行数)，然后修改完给出文件改动列表，用户可以一个一个同意或拒绝改动，或者一次性同意或拒绝所有文件改动，给出✅和❌选择
    
**4、支持图片输入，支持@某个文件的输入。
    **1)支持图片输入，使用base64编码，支持粘贴和选择路径
    **2)支持@某个文件的输入，可选择工作路径中的文件

**4、能够渲染opencode的输出，包括问题卡片、命令执行卡片、代码或者bash输出等的渲染。
    **1)问题卡片，
    **2)命令执行卡片，
    **3)代码或者bash输出，
**5、支持添加和列举现有skills、mcp。
    **1)skills可以自定义目录来加载
    **2)mcp可以自定义配置，支持各种协议

**其他需求，继续补充


提交物：
完整的插件包：提供vsix格式的文件包，需要验证正常可安装
